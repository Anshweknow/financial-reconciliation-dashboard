import { Moon, Sun } from 'lucide-react';import { useEffect, useState } from 'react';import { Button } from '../components/ui/button';import { Card } from '../components/ui/card';import { Input } from '../components/ui/input';import { useAuth } from '../contexts/AuthContext';
export function Settings(){const {user}=useAuth();const [dark,setDark]=useState(localStorage.theme==='dark');useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.theme=dark?'dark':'light'},[dark]);return <section className="grid gap-6"><div><h1 className="text-3xl font-bold">Settings</h1><p className="text-muted-foreground">Manage profile preferences and presentation settings.</p></div><div className="grid gap-6 lg:grid-cols-2"><Card><h2 className="font-semibold">Profile information</h2><div className="mt-4 grid gap-3"><label className="text-sm font-medium">Email<Input value={user?.email??''} readOnly/></label><label className="text-sm font-medium">Workspace<Input value="Default finance workspace" readOnly/></label></div></Card><Card><h2 className="font-semibold">Change password</h2><p className="text-sm text-muted-foreground">UI prepared for a future backend password endpoint.</p><div className="mt-4 grid gap-3"><Input type="password" placeholder="Current password"/><Input type="password" placeholder="New password"/><Button disabled>Update password</Button></div></Card><Card><h2 className="font-semibold">Theme</h2><p className="text-sm text-muted-foreground">Toggle light and dark mode.</p><Button className="mt-4" variant="secondary" onClick={()=>setDark(!dark)}>{dark?<Sun size={16}/>:<Moon size={16}/>} {dark?'Use light mode':'Use dark mode'}</Button></Card></div></section>}
