import React from 'react';
import { createRoot } from 'react-dom/client';

createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <p>Frontend scaffold only. Dashboard implementation is intentionally deferred.</p>
  </React.StrictMode>,
);
