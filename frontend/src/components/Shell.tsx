import { BarChart3, LogOut, Settings, Upload, WalletCards } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
import { Button } from './ui/button';
import { useAuth } from '../contexts/AuthContext';
const nav=[['/','Dashboard',BarChart3],['/upload','CSV Import',Upload],['/reconciliation','Reconciliation',WalletCards],['/settings','Settings',Settings]] as const;
export function Shell(){ const {logout,user}=useAuth(); return <div className="min-h-screen lg:flex"><aside className="border-b border-border bg-card/90 p-4 backdrop-blur lg:fixed lg:inset-y-0 lg:w-72 lg:border-b-0 lg:border-r"><div className="flex items-center gap-3"><div className="rounded-2xl bg-primary p-3 text-primary-foreground"><WalletCards/></div><div><p className="font-bold">Recon Finance</p><p className="text-xs text-muted-foreground">Enterprise control center</p></div></div><nav className="mt-8 grid gap-2">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2 text-sm font-medium ${isActive?'bg-primary text-primary-foreground':'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-8 rounded-xl bg-muted p-3 text-sm"><p className="font-medium truncate">{user?.email}</p><p className="text-muted-foreground">Authenticated workspace</p></div><Button variant="ghost" className="mt-4 w-full justify-start" onClick={logout}><LogOut size={16}/> Logout</Button></aside><main className="flex-1 p-4 lg:ml-72 lg:p-8"><Outlet/></main></div>; }
